{
  "name": "aegis-os",
  "version": "0.1.0",
  "private": true,
  "description": "Your AI Agent Operating System - Build agents with tools, RAG, MCP, and streaming",
  "author": "Aegis OS",
  "license": "MIT",
  "scripts": {
    "dev": "pnpm --filter agent-backend dev",
    "dev:web": "pnpm --filter web dev",
    "dev:all": "pnpm -r dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r clean"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=20.0.0"
  }
}

