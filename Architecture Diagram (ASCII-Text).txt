                                ┌───────────────────────────────┐
                                │           FRONTEND             │
                                │        Next.js 15 (UI)         │
                                └───────────────┬────────────────┘
                                                │
                                                │ tRPC / HTTPS / WS
                                                ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                        AEGIS OS — AGENT BACKEND                         │
│                   Node.js (Express/Fastify) + OpenAI SDK               │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│   ┌────────────────────┐       ┌───────────────────────────┐           │
│   │  Agent Orchestrator│◀──────▶   Agent Registry          │           │
│   │   (routing logic)  │       │ (configs, personas, tools)│           │
│   └─────────┬──────────┘       └──────────────┬────────────┘           │
│             │                                  │                        │
│             ▼                                  ▼                        │
│   ┌───────────────────┐           ┌───────────────────────────────┐     │
│   │  Reasoning Engine │           │   Agent Types (modules)       │     │
│   │   • ReAct loop    │           │  • convo-agent                │     │
│   │   • Tool planning │           │  • researcher-agent           │     │
│   │   • RAG inject    │           │  • orchestrator-agent         │     │
│   └─────────┬─────────┘           └──────────────┬────────────────┘     │
│             │                                      │                    │
│   ┌─────────▼─────────┐                  ┌──────────▼──────────┐        │
│   │    TOOL LAYER     │◀─────────────────▶      MCP GATEWAY     │        │
│   │  (JSON schema fn) │                  │ (Model Context Proto)│        │
│   ├────────────────────┤                  ├──────────────────────┤        │
│   │ http/fetch         │                  │  Filesystem host     │        │
│   │ database tools     │                  │  HTTP fetch tool     │        │
│   │ crawler/search     │                  │  Postgres connector  │        │
│   │ email/sms          │                  │  Custom external     │        │
│   │ filesystem tools   │                  │  services as MCP     │        │
│   └─────────┬──────────┘                  └──────────┬──────────┘        │
│             │                                         │                   │
│             │                                         │                   │
│             ▼                                         ▼                   │
│   ┌──────────────────────┐               ┌────────────────────────────┐   │
│   │    RAG PIPELINE      │               │       KNOWLEDGEBASE        │   │
│   ├──────────────────────┤               ├────────────────────────────┤   │
│   │ embeddings.ts        │               │ Supabase (pgvector)        │   │
│   │ chunking.ts          │               │ Qdrant / Pinecone optional │   │
│   │ retriever.ts         │               │ Document storage           │   │
│   └──────────────────────┘               └────────────────────────────┘   │
│                                                                         │
├─────────────────────────────────────────────────────────────────────────┤
│                      STREAMING LAYER (SSE / WS)                         │
│               Live tokens • Tool call events • Tool logs                │
└─────────────────────────────────────────────────────────────────────────┘

                                 ▲
                                 │
                                 │ streams status/logs/tokens
                                 │
                          ┌──────┴────────┐
                          │  CHAT UI      │
                          │ agent pages   │
                          │ tool logs     │
                          │ live output   │
                          └───────────────┘
🧠 What This Diagram Communicates
1. Separation of concerns

UI lives in apps/web

Agents + tools + MCP + RAG live in apps/agent-backend

2. Agents run inside a “Reasoning Engine”

This handles:

ReAct loops

Thought → Action → Observation cycles

Tool planning

Routing to the right agent type

3. Tools are modular

Each tool is:

JSON schema

Implementation

Registered in a “Tool Layer”

4. RAG pipeline is completely isolated

This helps you improve it independently:

Chunking

Embedding

Retrieval

Knowledge storage

5. MCP Gateway lets your agents use OS-level tools

Filesystem, external APIs, databases, crawlers — safely exposed.

6. Full streaming

The backbone that carries live:

Tokens

Tool calls

Tool results

Agent thoughts

Straight to your Next.js UI.